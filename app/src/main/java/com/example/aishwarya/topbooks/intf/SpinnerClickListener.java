package com.example.aishwarya.topbooks.intf;

/**
 * Interface that provides a callback when a spinner item is selected
 */
public interface SpinnerClickListener {
    void onSpinnerItemSelected(int position);
}
